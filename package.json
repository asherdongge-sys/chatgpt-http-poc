{
  "name": "chatgpt-http-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check src/chatgpt-http-client.mjs",
    "test": "node --test",
    "chat": "node src/cli.mjs"
  }
}
