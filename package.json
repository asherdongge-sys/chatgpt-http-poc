{
  "name": "chatgpt-http-poc",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "check": "node --check src/chatgpt-http-client.mjs && node --check src/gateway.mjs && node --check src/local-tools.mjs",
    "test": "node --test",
    "chat": "node src/cli.mjs",
    "gateway": "node src/gateway.mjs"
  }
}
